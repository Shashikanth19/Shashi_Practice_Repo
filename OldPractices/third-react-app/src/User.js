//Child component
function propsExample(props){
    return(
        <div>
            <h1>This is an example for props, name : {props.name}</h1>
            <h2>age : {props.age}</h2>
        </div>
    )
}

export default propsExample ;